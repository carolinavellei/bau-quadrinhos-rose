objeto: cachorro
função ou evento/método: latir, correr, lamber (realiza alguma ação de acordo com os objetos)
propriedades/atributos: pelo, rabo, focinho, pata (descreve o objeto)

cachorro.late(alto);
cachorro.corre(rápido);
cachorro.lambe(muito, molhado);

Colocar na tag <script></script> apenas objetos javascript

Se for colocar tags html, colocar dentro do <body></body>

tipos de dados

Inteiros (números positivos/negativos) - integer
Decimais 
Strings (texto) - dado do tipo texto entre aspas "" (pode escrever o que quiser, com caractere especial e tal);
Arrays (conjuntos) ["pera", "maçã", "laranja"] conjunto de valores strings. Índices apontam em qual posição cada string está posicionadado. A cada vírgula um índice. Começa a contar a partir do zero (O).
Objetos
Booleano - retorna dois valores: falso e verdadeiro. Quando preenche, por ex, um formulário o "sim ou não" se tornam "true or false".

nayra
erica
aline

Operadores lógicos

Operador

&& = retorna verdadeiro se as duas comparações forem verdadeiras

Operações de atribuição

var x = "";
x += "Mulheres ";
x += "podem ";
x += "programar!";
x; //"Mulheres podem programar!"

var x = 4;
x +="Anna";
x; //"4Anna";
x = "12";
x; // "12" (valor da variável é substituível)

Estruturas condicionais

if (2>1) {
  alert("É maior!");

}

alert("O que acontece comigo?");

Se a codição for verdadeira o alerta aparece.
Tudo que tá dentro da condicional fica dentro das chaves e executável.

Quando tá foa das chaves, aparece de qualquer jeito.

If, Else e Else if

if (condição verdadeira) {
  ...
} else {
  ...
};

if (condição verdadeira 1) {
  ...
} else if (condição verdadeira 2) {
  ...
} else {
  ... 
}


var nome = "Julia";
var meuNome = "Anna";

if (meuNome == nome) {
  alert("São iguais");
} else {
  alet ("Não são iguais");
}

if (meuNome == nome) {
  alert("São iguais");
} else if (nome == "Camila") {
  alert("Oi, Camila");

parseInt = converte string para número
toString = converte dados para string
















































